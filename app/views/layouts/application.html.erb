<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= "[dev]" if Rails.env != "production" %>HabitMachine | <%= "#{@page_title || controller.controller_name} > #{controller.action_name}" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/"><strong><%= current_user.fullname %></strong>は習慣の機械です(￣ー￣)v</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li class="<%= controller.controller_name == "habits" and controller.action_name == "result" ? "active" : "" %>"><%= link_to "記録結果", result_habits_path %></li>
              <li class="<%= controller.controller_name == "habits" and controller.action_name == "goal" ? "active" : "" %>"><%= link_to "目標", goal_habits_path %></li>
              <li class="<%= controller.controller_name == "diaries" and controller.action_name == "hilight" ? "active" : "" %>"><%= link_to "人生ハイライト", hilight_diaries_path %></li>
              <li class="<%= controller.controller_name == "senses" ? "active" : "" %>"><%= link_to "意識付け", senses_path %></li>
              <li><%= link_to "ユーザー情報", edit_user_registration_path %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>Habit Machine (Sakikazu Company 2012-<%= Date.today.year %>)</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%#= javascript_include_tag "application" %>

<script>
$(document).ready(function(){
  $(".colorbox").colorbox();
})
</script>

<!-- ページごとのJS
(memo: JSライブラリをlayoutの最下で呼び出しているので、各ページのJSはその場所に書いてしまうと、jQueryとかがまだ読み込まれていないのでエラーになる)
-->
<%= yield :addjs %>

  </body>
</html>
